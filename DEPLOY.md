# 🚀 Инструкция по деплою калькулятора

## Варианты деплоя

### 1. **Railway** (Рекомендуется) ⭐

Самый простой вариант - деплоит и фронтенд, и API вместе.

**Шаги:**
1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте новый проект
3. Выберите "Deploy from GitHub repo" или загрузите файлы
4. Railway автоматически определит Flask приложение

**Настройки:**
- Railway автоматически определит Python приложение
- **Port**: Автоматически используется переменная `PORT` (уже настроено в коде)
- **Start Command**: Автоматически определяется из `Procfile`

**Готово к деплою! ✅**
- ✅ `api_server.py` уже поддерживает переменную `PORT`
- ✅ `Procfile` создан для Railway
- ✅ Статические файлы будут отдаваться через Flask
- ✅ `calculator.js` автоматически определит правильный API URL

---

### 2. **Render** (Бесплатный) 🆓

Хороший бесплатный вариант с ограничениями на бесплатном тарифе.

**Шаги:**
1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте новый "Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python3 api_server.py`
   - **Environment**: `Python 3`

**Добавьте `render.yaml` в корень проекта:**
```yaml
services:
  - type: web
    name: exchange-calculator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 api_server.py
    envVars:
      - key: PORT
        value: 5001
```

---

### 3. **Vercel (фронт) + Railway/Render (API)** 🔀

Разделить фронтенд и API.

**Фронтенд (Vercel):**
1. Зарегистрируйтесь на [vercel.com](https://vercel.com)
2. Создайте проект из GitHub
3. Настройки:
   - **Framework Preset**: Other
   - **Build Command**: (не нужен, статика)
   - **Output Directory**: `web`

**API (Railway/Render):**
- Деплойте только `api_server.py` как отдельный сервис
- Обновите `API_URL` в `calculator.js` на URL вашего API

---

## Настройка для продакшена

### 1. Обновите `api_server.py`

Добавьте поддержку переменной PORT:

```python
import os
from flask import Flask, send_from_directory

app = Flask(__name__, static_folder='.', static_url_path='')

# ... остальной код ...

if __name__ == '__main__':
    port = int(os.environ.get('PORT', 5001))
    app.run(host='0.0.0.0', port=port, debug=False)
```

### 2. Добавьте CORS для продакшена (если фронт отдельно)

Если фронтенд на другом домене:

```python
from flask_cors import CORS

app = Flask(__name__)
CORS(app, resources={r"/api/*": {"origins": ["https://ваш-домен.vercel.app"]}})
```

### 3. Обновите `calculator.js`

Измените `API_URL` на продакшен URL:

```javascript
const CONFIG = {
    API_URL: 'https://ваш-api.railway.app',  // или другой URL
    USE_API: true,
    // ...
};
```

---

## Быстрый старт (Railway) ⚡

1. **Подготовьте проект:**
```bash
cd web
# Убедитесь что requirements.txt актуален
```

2. **Деплой на Railway:**
   - Зарегистрируйтесь на [railway.app](https://railway.app)
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo" (если есть репозиторий) или "Empty Project"
   - Если пустой проект: загрузите папку `web/` через Git или напрямую
   - Railway автоматически обнаружит Python приложение и `Procfile`

3. **Получите URL:**
   - Railway даст вам URL вида `https://your-app.up.railway.app`
   - Откройте этот URL в браузере
   - **Готово!** 🎉 Сайт работает!

**Что уже настроено:**
- ✅ Поддержка переменной `PORT` для продакшена
- ✅ Отдача статических файлов (HTML/CSS/JS)
- ✅ API endpoints (`/api/rates`, `/api/calculate`)
- ✅ Автоматическое определение API URL в JavaScript

---

## Переменные окружения

Если нужны API ключи (Binance, Doverka), добавьте их в настройках платформы:

**Railway:**
- Settings → Variables → Add Variable

**Render:**
- Environment → Add Environment Variable

---

## Рекомендация

**Лучший вариант для начала: Railway**
- ✅ Простота
- ✅ Автоматический деплой из GitHub
- ✅ Бесплатный пробный период
- ✅ HTTPS из коробки
- ✅ И фронтенд, и API вместе

Нужна помощь с конкретным вариантом? Скажите какой выбираете! 🚀

