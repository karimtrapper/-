# Лизхолд → Coins: независимый QA, 24.09.2026

## Итоговый результат


Основной стенд: SQLite `/tmp/stand_leaseholdqa170039.db`, порт 57572, STAND_MODE=1, внешние интеграции выключены. Полный путь выполнен в отдельных сессиях под реальными ролями marina, artem, vitaliy, teodor; админский переключатель роли не использовался. Связанная пара создана кнопкой «Тест лизхолда → Coins»: main СД-1487, small refund СД-1488.

| Шаг / роль | Ожидание | Факт | Статус |
|---|---|---|---|
| Marina s14→s15→s18 | 931 000 ₽ по совпавшим реквизитам; не пропустить конвертацию при `postConv=coins` | Приход #17, `incomeAmount=931000`, s18 | PASS |
| Artem s18→s18w→s22 | Пачка main 931 000 + small 100 000 ₽; центы сходятся | Всего 1 031 000 ₽, удержано 3 133 ₽ (контроль 1 071, наши 2 062), брокеру 1 027 867 ₽ по 81.40; два confirmed DEMO входа 7 576.42 + 5 050.94 = 12 627.36 USDT, распределение main 11 402.58 + small 1 224.78 = 12 627.36 | PASS |
| Artem s22→s23 | Main Coins 353 500 THB при 31.20; small refund 1 185.90 USDT | Main obligation 11 330.13 USDT, small 1 185.90 USDT | PASS |
| Vitaliy s23→s24 | Без refs не подписать; pending не считать отправкой | Кнопка до refs заблокирована; DEMO pending main 6 000 + 5 330.13 и small 1 185.90; `sendSum=0` | PASS |
| Teodor s24: pending, failed, partial | Не закрывать main и small | Pending оставил s24; failed small оставил долг открытым и без note; после одного confirmed main 6 000 оставался s24 | PASS |
| Teodor s24: весь подтверждённый факт | Закрыть refund и передать main на Coins, уведомления без дублей | После второго confirmed main и нового confirmed refund: main s25, small done/closed, `serverTransferComplete=true`, `serverSettled=true`, по одной note `stand:coins:1487` и `stand:refund:1488`; small profit 38.88 USDT | PASS |
| Artem s25: notify Coins | Показать фактические 353 500 THB, 11 330.13 USDT и оба ref; SCB не менять | `coinsNotified=true`, SCB 2 100 000 THB | PASS |
| Artem s25: manual credit | Без ref блок; amount/ref дают один кредит | Без ref блок; факт 353 500 THB и `DEMO-SCB-1487` → s26, SCB 2 453 500 THB | PASS |
| Artem s26: invoice | Без receipt блок, затем однократный SCB debit | До receipt блок; тестовый PDF, invoice paid 350 000 THB → s27, SCB 2 103 500 THB | PASS |
| Marina s27: close | Чек клиенту обязателен | До чипа «Чек отправлен клиенту» блок; после — `crmGaps=[]`, main done/closed, gross/net 184.63 USDT | PASS |

Экономика пары: 184.63 + 38.88 = **223.51 USDT**, как промежуточный plus пачки. Экспорт показывает фактический MF payout main 6 000 + 5 330.13 USDT, Coins credit 353 500 THB и invoice debit 350 000 THB; small reimbursement 1 185.90 USDT. Отдельный root commutator check по исходящим ref→dealId, сохранности ключей и происхождению: **3 PASS / 0 FAIL**.

**Фоновый poll без browser/API trigger — PASS.** Отдельный SQLite `/tmp/stand_poll_leaseholdqa.db`, порт 55103, `STAND_TRANSFER_POLL_ENABLED=1`: три sends подготовлены как pending с trusted DEMO outcome confirmed, main и small открыты, notes пусты, version 0. После запуска сервера 23 секунды не делал browser/API запросов. Прямой SQL показал version 1, verified 6 000 + 5 330.13 + 1 185.90 USDT, main s25/serverTransferComplete, small done/closed/serverSettled, ровно две целевые notes.

**Ранние FAIL исправлены и перепроверены.** Seed PUT первоначально отвечал 200/version19, но GET видел version18: backend перенёс `current_role()` до открытия DB session; новый seed сохранился, повтор PASS. Первоначальный s15 при предзаданном `postConv=coins` прыгал в s26: введён `invoiceDetailsReturn` только для возврата из s26; новый seed прошёл s15→s18, повтор PASS. Первые I1–I5 в разделе выше остаются PASS. I6: внешнего банковского адаптера нет, повторную доставку события банка проверить нельзя; доступен только ручной/test input (OPEN).

**Независимые тесты.** `node tests/test_stand_leasehold.js` — PASS (s15, статус отправки, verified amount, центы пачки, фактическая экономика, Coins/SCB). `python3 -m pytest tests/test_stand_transfer_adapter_qa.py -q` — 6 passed на mocked fixtures (confirmed, чужой recipient, неправильная сумма, failed, pending, invalid explorer URL). Pending/failed/partial в браузере не закрыли дела; idempotence notes и бухгалтерских проводок проверена состоянием и тестами.

**Свидетельства.** `qa-2026-09-24/leasehold-qa-s24-partial.png` визуально показывает 6 000 USDT confirmed и 5 330.13 pending на s24 без закрытия. `qa-2026-09-24/leasehold-qa-main-closed-overview.png` визуально показывает закрытую СД-1487 со статусом «Успешно завершена», направление leasehold→Coins и историю; фактические закрытые суммы main 184.63, small 38.88 USDT сверены с read-only `/tmp/leasehold-qa-final-state.json` (main/small/conv/notes/bal, без cookies/секретов).

**Ограничения.** Реальные переводы не выполнялись. Read-only TronScan у root дал HTTP 429, поэтому реальный mainnet confirmed success **OPEN**: DEMO и mocked fixtures его не заменяют. Внешний банковский connector отсутствует, фактическая доставка Telegram/Coins и реальное зачисление банком в STAND_MODE не проверены; локальные notes и ручной SCB credit проверены.

## История прогона (промежуточные статусы)


Локальный стенд: `http://127.0.0.1:57572`; `STAND_MODE=1`; SQLite `/tmp/stand_leaseholdqa170039.db`; сессия браузера `leaseholdqa170039`. Общая доска и платежные сети не используются.

## Подготовка

| Шаг | Ожидание | Факт | Статус |
|---|---|---|---|
| Запуск отдельного сервера и базы | Изолированный SQLite, свободный порт, внешние интеграции выключены | Порт 57572; `/api/health` 200; `/tasks` 302 до входа; стартовый лог: `[STAND] ... внешние интеграции выключены` | PASS |
| Готовность реализации | Новый demo mode и demo events доступны для безопасного прогона | Ожидается сигнал разработчика | open |

## План сквозного прогона

| Сценарий | Роли и действия | Проверяемое ожидание | Статус |
|---|---|---|---|
| RUB ожидание и приход | marina → artem: заявка leasehold, amount/account/purpose; частичный и полный RUB факт | Нельзя перейти без суммы, счёта, назначения; факт привязан к заявке; частичный приход не изображается полным | open |
| Пачка | artem: конвертация leasehold + мелкие возвраты | Суммы, удержание, доли, курс, прибыль сверены арифметически; каждое назначение имеет остаток | open |
| Мультисиг | vitaliy: refs и подпись; teodor: вторая подпись; artem: подтвержденный исходящий перевод | До двух подписей и actual confirmed success выдача/возмещение не закрыты; суммы и хеши из факта | open |
| Мелкий возврат | artem: успешный перевод фаундеру | Сделка и долг закрыты; прибыль не пустая, в заметке причина/результат | open |
| Coins и инвойс | artem: известить Coins фактическими суммами/хешами; оплата инвойса и чек; marina: чек клиенту и закрытие | Coins получает один точный набор сумм/хешей; чек обязателен; только менеджер закрывает после передачи чека клиенту | open |
| Отрицательные события | pending, failed, partial, повторный event, чужой recipient | Не закрывают сделку/долг и не дублируют уведомления | open |
| Роли и экономика | входы ролей, баланс SCB, Pay-In/Pay-Out, журнал | Правильные владельцы шагов; расход и приход не удваиваются; прибыль на всех сделках пачки | open |
| Сетевой адаптер | mocked fixtures отдельно от браузерного demo mode | Нет реального обращения к платежной сети и реальных переводов | open |

Формат результатов после прогона: шаг · роль · действие · ожидание · факт · PASS/FAIL/open. Для каждого FAIL будут точные условия воспроизведения и снимок/лог, если он нужен.

## Промежуточный браузерный прогон: RUB приход (24.09, отдельный стенд)

Предусловие: для проверки только этапа s14 в своей базе созданы 4 копии демо-сделки «Лизхолд / По реквизитам» с `step=s14`, ожидаемыми 100 000 ₽, счётом `…0286 · Сбер`, назначением `Договор QA9000N`, допуском 1 000 ₽. Подготовка фикстур сделана через JS консоль локального браузера; все действия сценариев — кликами и заполнением UI. Полный путь заявки до s14 этим прогоном не проверялся.

| Шаг | Роль | Действие | Ожидание | Факт | Статус |
|---|---|---|---|---|---|
| I1 · СД-QA90001 | manager | «Создать тестовый приход 100 000 ₽» | Одна тестовая запись, связь с делом, s15 | Поступление #9 `demo=true`, `dealId=90001`, `payinParts` 100 000 ₽, `incomeAmount=100000`, s15 | PASS |
| I2 · СД-QA90002 | manager | «Создать тестовую недоплату» | Ниже допуска → ручной разбор без признания полного прихода | Поступление #10 98 999 ₽; `incomeAmount=null`; s14m, причина «Недоплата 1 001 ₽» | PASS |
| I3 · СД-QA90002 | manager | «Привязать к сделке» без причины, затем с причиной | Без причины нельзя; с явной причиной сохраняется фактическая сумма | Первый клик: тост «Нужна причина привязки», s14m. После ввода причины: s15, `incomeAmount=98999`, причина в журнале | PASS |
| I4 · СД-QA90003 | manager | Зарегистрировать 60 000 ₽, затем 40 000 ₽, «Приход собран — дальше» | До второго платежа ожидание; после обоих сумма 100 000 ₽ и s15 | После первого `step=s14`, `incomeAmount=null`; после второго 2 части [60 000, 40 000], s15 и 100 000 ₽ | PASS |
| I5 · СД-QA90004 | manager | Зарегистрировать 100 000 ₽ с чужим счётом и назначением, попытаться закончить | Совпадения суммы недостаточно; нужна ручная причина | s14m, `incomeAmount=null`, две явные причины: неверный счёт и назначение | PASS |
| I6 | manager | Повторная доставка одного и того же прихода из внешнего банка | Не создаёт вторую часть и уведомление | Внешнего банковского адаптера нет; ручной/test input, см. итоговые ограничения | open |

Свидетельство: локальная доска `http://127.0.0.1:57572`, сделки 90001–90004, поступления #9–#13, журнал и состояние каждого дела проверены через браузерный `eval` после UI-действия. Эти номера существуют только в `/tmp/stand_leaseholdqa170039.db`.

## Полный browser demo: первое прохождение и блокер

После фикса сохранения seed создан через админскую кнопку и подтверждён отдельным GET: версия доски 20, СД-1485 (лизхолд s14) и СД-1486 (мелкий возврат). Вход `marina` — отдельная браузерная сессия. Точный тестовый приход 931 000 ₽ создал поступление #15, `incomeAmount=931000`, s15. Следующее действие менеджера «Передать операционисту на оплату» перевело СД-1485 **сразу в s26**, хотя конвертации/мультисига/Coins ещё не было. В отдельной сессии `artem` увидел s26. Это FAIL для полного пути; причина в предзаданном `postConv='coins'` у seed и ветке s15 `d.postConv?'s26':...`. Автору передано воспроизведение; исправления жду перед продолжением. Не считал s18+ проверенными.

