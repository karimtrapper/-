# Тест-план задачника — проверка флоу перед продом

Документ для QA-агента (`qa-tester`) и для человека. Цель — пройти все пути сделки
ролями, сверить цифры и найти то, что сломает работу людей, до того как задачник
поедет из ветки `stage` в `main`.

Правило одно: **факт получаем запуском, а не чтением кода**. Каждый пункт ниже —
действие в браузере и наблюдаемое ожидание. Непроверенное помечается `open`, а не
«наверное работает».

---


## Проверка лизхолда → Coins после доработки 24.09

Этот раздел задаёт актуальные условия. Старые сценарии ниже сохраняют историю
предыдущего прогона; сумму Coins теперь задаём с комиссией компании (в новом
DEMO: 353 500 ฿ вместо 350 000 ฿ для инвойса 350 000 ฿ и комиссии 1%).

Админ нажимает **«Тест лизхолда → Coins»**. Это отдельная пара DEMO-сделок;
существующая доска сохраняется. Начало — ожидание RUB на s14, документы учебные.

1. Менеджер: ожидание суммы, счёта и назначения. Зарегистрировать приход или
   создать помеченное тестовое поступление. При несовпадении — причина принятия.
2. Операционист: собрать пачку основной + маленькой, отправка брокеру и две части
   DEMO-прихода. В реальном режиме — полный хеш, сеть и серверная сверка прихода.
3. Назначение основной — Coins (инвойс + комиссия компании), маленькой — возврат
   Андрею. Виталий регистрирует отправки, можно несколькими частями, передаёт Теодору.
4. Теодор: в настоящем режиме подписывает **в кошельке**, сервер опрашивает сеть.
   Кнопка CRM только проверяет статус. В DEMO — кнопки исхода для каждой части.
   Pending/failed не закрывают. Полный возврат закрывает маленькую сделку с прибылью.
5. Когда вся пачка подтверждена, операционист получает s25: передать Coins фактические
   суммы и хеши. Затем внести зачисленные на SCB баты и референс банковской операции.
   Это ручное подтверждение по выписке; банковского автоконнектора на стенде нет.
6. s26: оплатить инвойс, приложить PDF/изображение чека. s27: менеджер отправляет чек
   клиенту и завершает; сервер создаёт уведомление с валовой и чистой прибылью.

Регрессии: повтор хеша/подтверждения, failed→замена, частичная отправка, чужой
получатель, смена адреса, недоступная сеть, преждевременное закрытие, две сессии,
неправильная роль, факт меньше ожидания, точное сохранение центов, чек без файла.

Ограничения стенда: SQL `Deal` и `StandState` пока разные хранилища; новые задачи
работают с `StandState`. Подписи ставятся во внешнем кошельке. Coins извещает
операционист; банк и Coins не подтверждают THB через API. Ранние этапы генерации
договоров не входят в этот прогон. Полноценный тестовый проход начинается с s14.

## 0. Как поднять стенд для прогона

Живую доску (`https://stand-app-production.up.railway.app`) прогонами **не трогаем**:
по ней ходят люди, а «Сбросить всё» чистит её у всех. Прогон — на локальной копии.

**Один прогон — свой порт, своя база, своя сессия браузера.** Два агента на одном порту
мешают друг другу: чужие входы съедают лимит, роль в браузере подменяется, а `kill` по
порту молча гасит чужой сервер (пойман на пробном прогоне 24.09). Поэтому порт берём
свободный и чужие процессы не убиваем:

```bash
cd Dev/CalcCRM-stage
RUN=qa$(date +%H%M%S)                      # метка прогона
PORT=$(python3 -c "import socket;s=socket.socket();s.bind(('',0));print(s.getsockname()[1])")
STAND_DOCPARSE_KEY=<ключ OpenRouter> STAND_MODE=1 DATABASE_URL=sqlite:////tmp/stand_$RUN.db STAND_PASSWORD=loc \
  PORT=$PORT SECRET_KEY=loc nohup python3 app.py > /tmp/stand_$RUN.log 2>&1 &
echo "$!" > /tmp/stand_$RUN.pid; echo "RUN=$RUN PORT=$PORT"
export AGENT_BROWSER_SESSION=$RUN          # в каждом вызове Bash — переменные не живут между вызовами
```

**Лимит входов: 5 в минуту** на всё приложение, шестой отдаёт 429 (в браузере выглядит
как «зависло на /login»). Входы через curl тоже в счёт. Между сменами ролей — пауза, или
держать cookie jar на каждую роль (`/tmp/$RUN-<логин>.jar`) и входить один раз: при том
же `SECRET_KEY=loc` cookie переживает и перезапуск сервера.

`STAND_TG_TOKEN` не задаём — локальная копия в Telegram не пишет.
Логины те же, пароль `loc`: `karim` (админ), `marina` (менеджер), `artem`
(операционист), `vitaliy` (фин дир), `teodor` (Теодор).

**Доска.** Два варианта:

- **Доска как в проде (Карим, 25.09):** пустая, без демо-сделок, без выдуманных чатов, клиентов и
  рефереров. Заявку заводят через «Чата нет в списке — вписать вручную» или «Без переписки». Паспорт
  и инвойс грузятся настоящими файлами; «Распознать документы» (или автоматически при передаче
  операционисту) — `/api/docs/parse` через OpenRouter, ключ стенда `STAND_DOCPARSE_KEY` (общий
  `OPENROUTER_API_KEY` на стенде погашен предохранителем). Для прогона — обезличенные документы,
  не реальных клиентов. Кнопками остаются только внешние события («Вебхук · демо»): приход рублей
  от банка, приход USDT на кошелёк, USDT от брокера (кладутся сами), подтверждение перевода в сети,
  курс дня. Переключателя «настоящие хеши / тестовый сценарий» нет — всегда вебхуки.
- Копия живой доски (чтобы проверять ровно то, что увидят люди) — только чтение с прода:

```bash
B=https://stand-app-production.up.railway.app; L=http://127.0.0.1:$PORT
curl -s -c /tmp/live.jar -X POST $B/api/auth/login -H 'Content-Type: application/json' \
  -d '{"username":"karim","password":"<STAND_PASSWORD из Railway>"}' >/dev/null
curl -s -b /tmp/live.jar $B/api/stand/state > /tmp/live_state.json
curl -s -c /tmp/loc.jar -X POST $L/api/auth/login -H 'Content-Type: application/json' \
  -d '{"username":"karim","password":"loc"}' >/dev/null
python3 -c "import json;d=json.load(open('/tmp/live_state.json'));json.dump({'data':d['data'],'version':0},open('/tmp/put.json','w'))"
curl -s -b /tmp/loc.jar -X PUT $L/api/stand/state -H 'Content-Type: application/json' -d @/tmp/put.json
```

В конце: `npx agent-browser close`, `kill $(cat /tmp/stand_$RUN.pid)` — только свой сервер.

## 1. Как ходить по экранам (грабли автоматизации)

- Браузер — `npx agent-browser` в сессии `$RUN` (см. раздел 0).
- **Вход по паролю.** Форма `#pwForm` скрыта, а «Вход по паролю» — переключатель: второй
  клик её прячет. Ссылки `@eN` из snapshot после этого протухают. Надёжно так:
  `open $L/login` → `wait 1500` → `click '#pwToggle'` → `fill '#loginUsername' <логин>` →
  `fill '#loginPassword' loc` → `click '#loginBtn'` → проверить, что адрес стал `/crm`.
- **Выход:** `eval "fetch('/api/auth/logout',{method:'POST'})"` — только с открытой
  страницей стенда; в пустой вкладке (about:blank) падает «Failed to parse URL».
- **После входа убедиться, что `S.role === standMe`** (`eval "S.role+' / '+standMe"`). Браузер
  автоматизации иногда восстанавливает из кэша страницу прошлого пользователя — тогда
  свой шаг показывается как чужой («Сейчас ход роли …»). Лечится повторным `open` задачника.
  У живого человека такого нет: роль ставится с сервера до первой отрисовки.
- Переключатель ролей только у `karim`, кнопки подписаны со счётчиком («Менеджер 2») —
  искать по началу текста. Остальные видят одну неактивную плашку своей роли.
- **Прямой переход** `/tasks?deal=<id>` открывает «Карточку сделки» (не задачу) и сразу
  стирает параметр из адреса. Шаг — вкладка «Текущая задача».
- Кнопки искать `find text "…" click`; селектор `button:has-text(...)` agent-browser не понимает.
- **Поля с `onchange`, а не `oninput`**: после `fill` нажать `Tab`, иначе значение не
  попадёт в состояние. Проверять себя через `eval "JSON.stringify(S.deals.find(d=>d.id===1479))"`.
- Стабильные id полей на шаге «Что делаем с USDT»: `#pc_<id>` назначение,
  `#cr_<id>` курс Coins, `#ct_<id>` баты, `#cad_<id>` адрес, `#ca_<id>` сумма USDT,
  `#cw_<id>` кошелёк возврата фаундеру. На шаге «Отправить с кошелька»: `#sh_<id>`
  ссылка на транзакцию, `#sa_<id>` сумма, кнопка `button[onclick='sendAdd(<id>)']`.
- Селекторы по порядку (`combobox` №3) ненадёжны: после выбора назначения в карточке
  появляются новые селекты (сеть, кошелёк), и порядок сдвигается.
- Диалоги: «Вернуть с вопросом» открывает `prompt`, «Сбросить всё» — `confirm`. Сначала клик, потом отдельной командой `agent-browser dialog accept "<текст>"`; пока диалог открыт, `eval` и `snapshot` не работают.
- Тосты читать так: `eval "Array.from(document.querySelectorAll('[class*=toast]')).map(e=>e.innerText).join(' | ')"`.
- Уведомления: `eval "JSON.stringify(S.notes.slice(-6).map(n=>[n.role,n.text]))"` —
  кому и что ушло.
- Скриншот в полный рост рисует шапку посреди страницы — это артефакт снимка, не баг.

## 2. Эталонные формулы — по ним сверяем цифры на экране

| Что | Формула | Где в коде |
|-----|---------|------------|
| Удержание при отправке брокеру | валютный контроль 0,1 % + 40 ₽ (уходит наружу) + 0,2 % (остаётся на рублёвом счёте) — от всей суммы пачки | `BROKER_FEE`, `brokerSend` |
| Доля сделки в пачке | удержание делится по доле рублей сделки; доля USDT = (рубли сделки − её часть удержания) / курс брокера | `convMargin` |
| Ожидаем USDT | (рубли − удержание) / курс брокера | шаг s18 |
| Должны выдать | сумма батов всех сделок пачки; для мелких без батов — рубли / средний курс дня | `convMargin`, `approx` |
| Порог безубыточности | отправлено брокеру / (баты / курс USDT→THB) | `convMargin` |
| USDT для Coins | баты / курс Coins | `transferSet` |
| СБП-приход | в сделку идёт брутто: зачислено + комиссия эквайринга 0,96 % | ручной разбор прихода |
| Допуск прихода | по умолчанию 1 000 ₽, правится на шаге «Ждём приход» | `expect.tol` |
| Курс уехал | расхождение курса сделки с курсом дня больше 0,5 % → предупреждение и «Запросить курсы заново» | `RATE_DRIFT` |
| Стартовые балансы | IPPS 1 250 000 ฿ · касса 480 000 ฿ · SCB 2 100 000 ฿ · USDT 41 800 | `balInit` |

Контрольный пример (демо-засев после правок 24.09): очередь — 5 обменов на 754 000 ₽
(Полякова 238 000, Мартиросян 64 000, Ким 156 000, Гурьев 84 000, Соколова 212 000).
Пачка СД-1479 (931 000 ₽, 350 000 ฿, клиенту 2,66) + 5 мелких = 1 685 000 ₽ → удержано
5 095,00 ₽ (1 725,00 + 3 370,00) → брокеру 1 679 905,00 ₽ → по 81,40 = $20 637,65; должны
выдать 639 999 ฿ = $20 512,79 по 31,2 → плюс $124,87 (0,61 %), порог 81,9. Доли: СД-1480
$1 028,82 · СД-1476 $2 596,55. Coins 350 000 ฿ по 31,20 = 11 217,95 USDT; 81 538 ฿ по
31,00 = 2 630,26 USDT (Соколова в минусе на $33,71 — должно быть предупреждение).

## 3. Сценарии прогона

Каждый сценарий проходится ролями по очереди на **своей** сделке — заявку заводит
`marina` через «+ Новая задача». Демо-сделки используются только там, где сказано.
На каждом шаге проверяется: кому ушла задача (`S.notes` + доска нужной роли), что
записалось в журнал, совпадают ли цифры с разделом 2 и счётчик «N из M».

| # | Сценарий | Роли по порядку | Главное, что проверить |
|---|----------|-----------------|------------------------|
| S1 | Лизхолд, по реквизитам, новый клиент, мультисиг | marina → artem → marina → artem → marina → artem → vitaliy → teodor → artem → marina | путь «N из 15», все шаги от «Ответить курс» до «Чек клиенту и закрытие»; паспорт и инвойс обязательны; реквизиты менеджера доходят до оплаты с копированием; чек обязателен; закрытие неактивно до «Чек отправлен клиенту» |
| S2 | Лизхолд, старый клиент | как S1 | обязателен только инвойс; строка «Загранпаспорт» видна с «не обязателен»; вместо договора — приложение и счёт |
| S3 | Фрихолд | как S1 | 0,8 % + $50, «Дойдёт застройщику», один карман |
| S4 | Аренда | как S1 | только тип-чип, отдельных полей нет (вопрос открыт — фиксируем как есть) |
| S5 | Крипта (лизхолд или обмен USDT→THB) | marina → artem → … | нет брокера, конвертации и «Ждём USDT»; в журнале «Конвертация не нужна»; курс клиенту — ฿ за 1 USDT (50 000 ฿ / 31,05 = 1 610,31 USDT); приход по хешу TRC-20 с допуском ±5 USDT; сделка не попадает в рублёвую очередь (B5) |
| S6 | Обмен «Курс знаю — сам» | marina | сразу «Отдать расчёт клиенту · 1 из 13», операционисту задачи нет, в журнале «Курс проставил менеджер сам» |
| S7 | Курсы вразнобой | artem → marina | Asia 81,70 → Tradex 81,40 → Екатерина 80,90: метка «лучший» переезжает; первый ответ по обоим направлениям отдаёт задачу менеджеру; опоздавший курс перебивает лучший; «взять этот» → «выбран вручную», операционисту уведомление |
| S8 | Курс уехал, клиент думал | marina | на отправке клиенту симуляция +1 % → «Курс уехал на 1,0x %»; «Запросить курсы заново» очищает ответы, операционисту «♻️ Повторный запрос №N» с причиной |
| S9 | Приход частями и чужим плательщиком | marina | «Забрать приход руками»: пул поступлений + ручная строка; сошлось в допуске → реквизиты; не сошлось → «Разобрать поступление» с обязательной причиной; СБП с комиссией 0,96 % |
| S10 | Пачка конвертации и все назначения | artem → vitaliy → teodor → artem → marina | демо СД-1479 + очередь. «Взять все»; удержание и доли по разделу 2; каждое из пяти назначений хотя бы на одной сделке; частичная отправка (сумма + остаток); подписи 1 из 2 → 2 из 2; извещение Coins с суммой отправленного (B3); оплата и закрытие главной (B1, B2) |
| S11 | Личный кошелёк вместо мультисига | artem → teodor | при выборе «Кошелёк Теодора» шаг второй подписи выпадает («8 из 14»); отправляет владелец с хешем |
| S12 | Сбои | разные | брокер прислал меньше → «не хватает $…» и журнал «Расхождение с заявкой»; «Брокер задерживает»; Теодор «Отклонить с причиной»; «Вернуть с вопросом» на каждом шаге, где он есть (s5, s11, s24, s26); закрытие с причиной на любом шаге |
| S13 | Многопользовательский режим | два браузера | правка одного видна другому за пару секунд без перезагрузки; одновременные нажатия не затирают молча (409 → свежая доска и кто подвинул); одно событие = одно сообщение в Telegram |
| S14 | Права ролей | все логины | переключатель ролей только у karim; «Сбросить всё» и «Добавить примеры» только у karim, `POST /api/stand/reset` другим — отказ; чужой шаг показывает «Сейчас ход роли …» без совета переключаться (B10) |

## 4. Правила по этапам — чек-лист

Последние решения Карима на 24.09. Отменённое в скобках, чтобы не принять старое
поведение за баг и наоборот.

**Заявка и клиент**
- Заявка — один экран (не мастер «Шаг 1 из 2»). Источник обязателен: Telegram / WhatsApp / Битрикс / «Без переписки»; живой поиск по подстроке («мар» → Мартиросян и Соколова Мария), счётчик «Найдено N из M», фокус не теряется, смена канала сбрасывает поиск.
- После выбора чат сворачивается в строку «ОТКУДА КЛИЕНТ · …» с «Сменить источник» и «Оставить как было». «Чата нет в списке — вписать вручную» + «Привязать» → пометка «введён руками», клиентом становится ник (решено 24.09).
- Клиент только из справочника, имя руками не вводится. Нет карточки → имя из чата + «Создать карточку»; при сохранении создаётся клиент и `clientId`.
- Смена канала сбрасывает только автоматически узнанного клиента; выбранного руками не трогает. «Взять из карточки» подставляет и чат, и канал (номер WhatsApp не уходит в Telegram).
- Новый / старый клиент — автоматически по наличию договора (плашек «впервые» нет, история в карточке клиента).
- Тип: «Оплата недвижимости» (Лизхолд / Фрихолд / Аренда обязательно) или «Обмен валюты» (пары RUB→THB, USDT→THB, RUB→USDT, THB→USDT; USDT→THB оставляет только «Крипта», RUB→USDT — только рублёвые способы). «Перевод за товар» убран.
- Сумма обязательна. Вторая сумма — прикидка «≈» по курсу дня, не сохраняется.
- Кнопки «Создать заявку — курс спросит операционист» (сразу «Ответить курс», уведомление операционисту) и «Курс знаю — сам».
- Агенты: один блок «Агенты сделки», уровни, модели revshare / markup / fixed / % от прибыли в крипте, пресеты «Каскадом» и «В долю». Автоподстановка только при следе (метка Битрикса, закреплённый партнёр, прошлая сделка из чата с агентом). Нет следа — спокойная строка без плашек. Revshare не уходит ниже 0. У проваленной сделки начисление 0, но она входит в конверсию реферера. Пример Карима: 1 000 000 ₽ → брокеру 996 960 ₽ = 12 247,67 USDT, выдача 11 880,24, прибыль 367,43 (3 %), revshare 30 % = 110,23 $, markup 1 % = 122,48 $.

**Курсы**
- Контрагенты: RUB→USDT — Крипта-Платежи (Екатерина), Asia Capital, Tradex; USDT→THB — Coins.co.th, Bitazza (запасной). PRO100TRADE убран отовсюду. Брокер по умолчанию Tradex.
- Список редактируемый: «+ Добавить контрагента», «убрать» с подтверждением, «чат» для правки привязки. Личный аккаунт — «перейти с вопросом», группа — «перейти», вопрос в буфере.
- Вопрос брокеру «Какой курс на 2 850 000 ₽?», Coins «Which rate? usdt-thb, 1 000 000 THB» — без приписки «сумма примерная».
- У каждого контрагента строка и «Отправить» (не «Записать» и не общее «Отдать курсы менеджеру»). Отправленное сворачивается, есть «Редактировать» / «Сохранить» / «убрать». Журнал «Курс получен — …», «Курс обновлён — …».
- Лучший: RUB→USDT меньший, USDT→THB больший. Блок «Курсы контрагентов» у операциониста виден до прихода денег, после прихода пропадает.
- Уведомления менеджеру: «Новый курс · X — теперь он лучший в сделке», «Курс обновлён · …»; «прежний лучший» — снимок до правки. Операционисту: «запросил заново» (с причиной, ответы очищены) и «взял не лучший».

**Расчёт клиенту**
- Курс и сумма клиенту связаны в обе стороны: 2,85 × 5 000 000 ฿ = 14 250 000 ₽; 14 000 000 ₽ → курс 2,8. Брокер 81,10 → 79,50: себестоимость 2,5994 → 2,5481, наценка +9,64 % → +11,85 %, сумма клиенту не меняется. Вставка «2.568221 ₽/฿» чистится до числа.
- Кнопки «Клиент согласен», «Запросить курсы заново», «Клиент отказался». «Открыть в калькуляторе» передаёт курсы, сумму, тип и метод «Кастом» параметрами адреса.

**Документы и договор**
- Новый клиент: паспорт и инвойс обязательно, SPA и анкета ИПДС — если есть. Старый: только новый инвойс, строка паспорта видна.
- «Загрузить» явной кнопкой; клик по строке ничего не снимает. После загрузки имя · размер · время, «Открыть», «Убрать».
- Расхождение имён паспорт / SPA — жёлтое предупреждение, не блок. Машиночитаемая строка паспорта отображается.
- Договор: блоки по источнику (паспорт, инвойс, «ГЛАВНОЕ · СУММА И КУРС», «курс зафиксирован» со временем). Обязательны ФИО, паспорт, объект, сумма, курс; «700 000» сохраняется без NaN. PDF / DOC / «Загрузить свой» (версия +1) / «Вернуть сгенерированный».
- Отправка клиенту: реквизиты MF Corp с копированием по строке и «Скопировать всё»; подписанные документы необязательны, несколько файлов, снимаются при смене условий. На этом шаге нет полей курса и пересборки — только «Запросить курсы заново». Проверка курса дня: ≤ 0,5 % «Курс держится», больше — «Курс уехал на …» с направлением.

**Приход и реквизиты**
- «Ждём приход»: сумма, счёт, допуск, назначение редактируются; правка назначения помечается «Изменено вручную» с «вернуть как в договоре»; каждая правка — в журнал «было → стало».
- Ручной разбор, СБП 0,96 %, s14m с обязательной причиной — см. S9. Молча с недобором дальше не едет.
- «Реквизиты для оплаты» у менеджера сразу после прихода: получатель, банк, счёт, сумма, инвойс, срок, назначение для банка получателя («Payment for unit B1204, invoice INV-4072, Gusev Igor»). Без банка, счёта и назначения шаг не проходит.

**Конвертация, назначения, подписи**
- «Отправка брокеру»: шапка — объём всей пачки; обязательны брокер, курс, кошелёк; проверка маржи «Работаем в плюс / Уходим в минус» с порогом безубыточности по всей пачке, запись «Проверка перед отправкой: …».
- Удержание 0,3 % + 40 ₽ на пачку; 0,2 % в прибыль не входят (строка «Не в счёт сделки»). Проверки: 474 292,27 → удержано 1 462,88 → отправлено 472 829,39; 141 405,13 → 464,22 → 140 940,91.
- «Ждём USDT»: сумма не предзаполнена; перевод из входящих, несколько суммируются; ручной хеш сперва ищется по подстроке во входящих; иначе «сумма по заявке» / «сумма со слов»; повторный хеш не добавляется. Прибыль от факта, при пачке факт делится по доле рублей.
- Взятые мелкие исчезают из очереди и в другую пачку не попадают; курс пачки пишется во все её сделки. Крипто-сделки в рублёвую очередь не попадают вовсе.
- **Задачи заполняют карточку CRM (Карим, 24.09).** Каждый шаг пишет те же поля, что заполнили бы руками в CRM: Pay-In → конвертация (USDT, хеши) → Pay-Out (источник, баты, USDT, хеши выдачи). Возмещение — только когда баты выдал фаундер своими; выдача деньгами, которые пришли к нам, — просто расход в Pay-Out.
- **Pay-Out по назначению.** Coins → источник «Coins / Bitazza», баты, курс, после сети — USDT и хеш выдачи (у недвижимости ещё строка в «Переводы в MF Corp (факт)»). Клиенту на кошелёк → «USDT клиенту на кошелёк», USDT и хеш. IPPS → «Баланс IPPS», баты, баланс IPPS уменьшается. «Оставить на кошельке» → обязателен выбор «Откуда выдали баты» (касса / SCB), баланс уменьшается. Вернуть фаундеру → «Кошелёк фаундера», фаундер, «выдал свои баты», USDT к возврату; возмещение #R-<id> закрывается хешем после сети, либо сразу «автовозмещение», если USDT от брокера пришли на кошелёк этого же фаундера. Оплата инвойса с SCB дописывается строкой «Оплата инвойса», Pay-Out не затирает.
- **Закрытие мелких сделок пачки — как в CRM: когда клиент получил деньги.** При получении USDT стоят «В пачке — ждёт выдачи». IPPS, касса/SCB и возврат фаундеру (баты уже выдал фаундер) — закрываются сразу после решения по USDT; долг фаундеру живёт во вкладке «Возмещения». Coins и перевод клиенту — после ухода перевода в сеть.
- **Кошелёк клиента и приход частями у крипты (Карим, 25.09).** На «Отправить клиенту» (s12) необязательное поле «Кошелёк клиента, с которого придёт USDT» — только адрес TRC-20 (`T` + 33 символа), иначе тост и поле не сохраняется. На «Ждём приход» (s14) тот же адрес `#pwc_<id>`, поле хеша `#ph_<id>` и «Проверить в сети» → `POST /api/stand/payin/check`: сервер ищет перевод в TRON на наш кошелёк сделки, сумму и отправителя берёт из сети, отметку `verified` ставит только он (браузерный PUT её не подделает и сумму не перепишет). Пустой кошелёк клиента заполняется отправителем первого проверенного хеша. Перевод с другого адреса помечается «не кошелёк клиента» и не засчитывается, пока менеджер не нажмёт «Это клиент — засчитать». Дубль хеша (в любой сделке или пачке) — 409, перевод раньше создания сделки — 422, рублёвая сделка — 404, сделка не на s14 — 409. Сведение после каждого перевода: получено ≥ ожидание − 5 USDT и ≤ ожидание + 5 → s15; меньше → остаёмся на s14 с «ждём ещё N»; больше → s14m «Переплата». Кнопка «Больше не придёт — разобрать недобор» → s14m «Недоплата». Демо-вебхуки: «Тестовый перевод 1 USDT» (пока переводов нет), «Пришло ровно / остальное N», «Пришло на 10 USDT меньше». Кошелёк клиента уходит в CRM в `notes`.
- **Очередь Карима 25.09 (вечер).**
  - Параллельно после прихода: `afterPayin` — рубли сразу на s18 (отправка брокеру), крипта на s22; у менеджера параллельная задача «Реквизиты для оплаты» (`reqTask='open'`, в «Мои задачи», экран — форма s15 с «Сохранить реквизиты», шаг не меняется). s15 из пути убран (кроме старых сделок на нём). Реквизиты ждут только на s26: «Вернуть с вопросом» с s26 открывает задачу менеджеру, шаг остаётся s26. Менеджер может «Изменить реквизиты для оплаты», пока инвойс не оплачен.
  - Ошибочный подтверждённый приход брокера: «убрать» → причина → `POST /api/stand/incoming/unlink` (операционист/фин дир/админ, пока по пачке нет отправок). Принятая пачка возвращается на s18w, доли прихода обнуляются. На s22 то же через «Не тот приход… — отвязать». На s18w без подтверждённых приходов можно сменить кошелёк пачки («Брокер прислал на другой кошелёк?»).
  - Coins: назначение «Конвертация USDT для отправки батов через партнёров (Coins)»; под батами — «N ฿ получателю + M ฿ комиссия компании (P %)», при P < 1 % — предупреждение без запрета. На s25 расхождение фактических батов с заявкой не запирает шаг: пишется в журнал, причина необязательна.
  - Знакомый клиент: выпуск пакета ставит клиенту `docs=true` → следующая сделка `isOld`, паспорт не обязателен, паспортные поля договора берутся из прошлой сделки клиента.
  - «Есть сумма» — якорь: ввод курса/суммы на s6 пересчитывает баты, а не сумму клиента (50 000 USDT × 33 = 1 650 000 ฿).
  - `/api/stand/prod-agents` — только админ и менеджер.
- **Фидбэк живого теста 25.09 (вторая половина дня).**
  - Синхронизация: опрос раз в 2,5 с не перерисовывает экран 1,5 с после нажатия (клик не теряется); набранное в полях без обработчиков (комментарии, ответы курса `q_*`, поля пакета `df_*`, хеш) переживает перерисовку до перехода шага; при 409 (доску поменял коллега) правки сливаются по сделкам — меняли разное → остаются обе, одну сделку → версия сервера и тост; две новые сделки с одним номером → вторая получает следующий номер.
  - Операционист s5: «Что уйдёт в сделку» — текст, не поле ввода, с подписью «заполнять не нужно».
  - s6: плейсхолдеры «впишите курс» / «или впишите сумму» вместо чисел, похожих на значения.
  - Создание задачи: чат не обязателен; при пустом списке чатов — подсказка «впишите вручную или без привязки».
  - Назначение платежа (рубли): обязательное поле менеджера на s8 (наверху, кнопки «По агентскому договору» / «По инвойсу …»), без него «Передать операционисту» не проходит; на s11 — первое поле после суммы/курса, обязательное (`purpose` в `docMiss`). У крипты поля нет.
  - s14m: список привязанных приходов с «Отвязать» и кнопка «Не тот платёж — вернуться и выбрать другой» → s14, приходы снова свободны в «Поступлениях». На s14 у крипты любой перевод можно «убрать».
  - Курс дня — настоящий рынок `/api/rates` (RUB→USDT / USDT→THB), не выдуманный и не случайный; «Рынок ушёл на N %» считается от рынка в момент фиксации курса (`d.rates.mkt`), без снимка предупреждения нет. Демо-кнопки «−1 % / +1 %» убраны.
  - Агенты: `POST /api/stand/prod-agents` берёт рефереров прод-CRM по `STAND_PROD_RO_KEY` (только чтение), без TEST-профилей и без токенов кабинетов, заводит их в CRM стенда (`is_test`); при загрузке, если прод-агентов в справочнике нет, подтягиваются сами; у админа кнопка «Агенты из прода».
- **Ожидание от кошельков (Карим, 25.09, вторая итерация).** На s12 у крипты блок «Как ждём приход»: наш кошелёк + «С кошелька клиента» (обязательно) либо галка `#pwu_<id>` «Клиент не прислал адрес — привяжу перевод по хешу». Без одного из двух «Ждём приход» не пускает (тост). Наблюдатель (демо-вебхук) узнаёт перевод клиента только по адресу: с кошелька клиента — идёт в сумму; кошелёк не указан или «с другого кошелька» — строка «не опознан как клиент / не кошелёк клиента — в сумму не идёт» и две кнопки «Это клиент — засчитать» (при пустом кошельке клиента подставит адрес отправителя) / «Не наш — убрать». «Получено» считает только засчитанные. Хеш добавляется вручную всегда — «Добавить перевод по хешу». Поле «Срок оплаты» на s15 убрано (платим сразу).
- **Кошелёк прихода у крипты (Карим, 25.09).** На «Подготовить договор» — выбор «Кошелёк, куда клиент платит USDT» из справочника (Груши / Виталия — мультисиг, Теодора / Андрея — личные); адрес подставляется в «Куда платит клиент» и на «Отправить клиенту». На «Ждём приход» кошелёк можно сменить, пока денег нет; после сборки пакета адрес в договоре не переписывается, а появляется предупреждение «В договоре у клиента другой адрес…». Кошелёк без корректного адреса TRC-20 (Андрей — «адрес уточнить») выбрать нельзя. Дальше путь по кошельку: личный — s23 у владельца без второй подписи.
- **Закрытие = сделка в CRM (Карим, 25.09).** На «Чек клиенту и закрытие» блок «Что уйдёт в CRM» (клиент, тип, Pay-In с хешами, инвойс и курсы / Pay-Out, переводы выдачи, статус) и кнопка «Проверил — внести в CRM и закрыть «Успешно»»: POST /api/deals в CRM стенда со статусом completed, в журнале «Внесена в CRM: сделка #N … прибыль по CRM $X», в карточке закрытой сделки «В CRM: сделка #N — открыть в CRM» (`/crm?deal=N` открывает детали). Прибыль CRM должна совпасть с задачником: лизхолд/крипта — до цента (СД-1485: $155,59 и там и там).
- Вкладка «Возмещения»: «Не возмещено» (сделка, фаундер, баты, сколько вернуть, «ждёт перевода») и «Возмещено» (как закрыто, хеш).
- Сумма к отправке больше доли сделки — жёлтое предупреждение в карточке назначения: для Coins «будем в минусе», для остальных «лишнее уйдёт из денег других сделок пачки».
- «Известить Coins» — таблица по всем сделкам пачки с назначением Coins: баты, курс, сколько USDT реально ушло, хеши.
- Во входящих на «Ждём USDT» только переводы после отправки рублей; стенд сам кладёт два перевода брокера (60 % и остаток) через 1 и 2 минуты после отправки.
- Назначения по каждой сделке: Coins (курс + баты → USDT: 640 000 / 31,20 = 20 512,82), IPPS (задачи владельцу кошелька нет, «Возврат USDT» по умолчанию «не нужен»), «Оставить на кошельке», «Вернуть фаундеру» (кошелёк из списка), «Клиенту на кошелёк» (сумма и адрес). Сеть TRC-20 / ERC-20 обязательна и попадает в текст задачи. Сводка «пришло · уйдёт · останется». Без назначения шаг не проходит и называет сделку.
- Подписи: мультисиг — Виталий «Подписать N переводов — дальше их одобрит Теодор», затем Теодор «Одобрить вторым ключом»; личный кошелёк — владелец сам с хешем. Отправка частями, «Отправлено $X из $Y», «Закрыто: N из M»; «Подписал всё» блокируется с указанием сделки без ссылки. Путь не укорачивается задним числом: на оплате «14 из 15», на закрытии «15 из 15».
- Coins платит только на счёт MF Corp; оплата инвойса со счёта SCB без выбора источника, чек обязателен (несколько файлов); закрытие — «Чек клиенту и закрытие», кнопка неактивна до отметки «Чек отправлен клиенту».

**Уведомления, роли, стенд, UI**
- Telegram: бот `@grusha_stand_bot`, группа «тесты на задачи». Формат «🔔 Артём · операционист · Задача на вас: Ответить курс · СД-1475 · Гусев Игорь», «СД-… · клиент» — ссылка прямо на задачу. Первая задача заявки тоже уведомляет. Одно событие — одно сообщение.
- Роль ограничивает только задачник, в CRM все видят всё (решено, не баг). Смена роли в «Аккаунты стенда · роли» применяется без перелогина.
- Нет горизонтального выезда на 1440 / 1280 / 1024 / 768 / 390; текст не мельче 11,5 px; кнопки на телефоне не меньше 38 px. «Комментарий к шагу» есть на каждом шаге и уходит в журнал.
- Хеши кликабельные (tronscan / etherscan) с копированием, адреса кошельков целиком, банковские счета маскируются (…0286).
- Отказ всегда объясняет: тост с именем поля, подсветка, список недостающего, прокрутка к первому пустому полю.

## 5. Регрессия — баги, которые уже чинили

Прогоняются при каждой проверке перед продом. Если вернулся — FAIL с пометкой «регрессия».

1. «платит undefined» у старых сделок. 2. Поле `pay` затирало данные на шаге фин дира. 3. Пропадала машиночитаемая строка паспорта. 4. Поиск чатов терял фокус. 5. Из общего чата молча подставлялся последний человек. 6. «Обмен валюты · Лизхолд» и зашитый RUB→THB у крипты. 7. «Дольше всего: Закрыта 12 ч». 8. «— ₽ за 1 ฿» без курса. 9. Revshare в минус. 10. Карточка клиента и плашка считали сделки по-разному. 11. После удаления агента уровни начинались со второго. 12. Сохранение обнуляло скрытые поля (сумма, комиссия, сумма инвойса, курс продажи). 13. «+ ещё приход» / «+ транзакция» стирали ввод. 14. «копия» у шаблонов не копировала. 15. Похожие сделки по чату находили саму сделку. 16. «Заполните обязательные поля» без объяснения. 17. Приход USDT делился без вычета удержания. 18. 0,2 % попадали в прибыль. 19. Сумма USDT была предзаполнена ожиданием. 20. «По курсу — это —» в задаче Coins. 21. Зашитые TRC-20, кошелёк Виталия, обрезанные адреса. 22. Кошелёк влиял на подпись только после решения по USDT. 23. Падение на значении не из справочника. 24. «1 697 252,8 ₽» и «0.1%». 25. «Компания подписывает первым». 26. «у роли «—»». 27. Коллизия `money2`. 28. Модалка пачки шире экрана. 29. Выезд вбок на телефоне (rub-23/24/23p/25/27, кнопки PDF/DOC). 30. Узнанный клиент оставался после смены канала. 31. Номер из карточки писался в чужой канал. 32. Привязка чата подменяла клиента, выбранного руками. 33. Лизхолд сбрасывал способ оплаты на «По реквизитам». 34. Предзаполненная сумма клиенту не пересчитывалась. 35. NaN из «700 000». 36. Повторный запрос у «Курс знаю — сам» вёл на удалённый шаг. 37. `screens-data.js` застревал в кэше. 38. Курс Coins ничего не считал. 39. «13 из 13» задним числом. 40. «лучший прежний» читал изменённый объект. 41. Панель уведомлений уезжала за край на телефоне. 42. Ручной забор СБП терял 0,96 %. 43. Зашитое «меньше на 1 000 ₽». 44. Клик по строке документа снимал файл. 45. Строка паспорта скрывалась у знакомого клиента. 46. Копеечное расхождение долей пачки. 47. Суффикс «₽/฿» в поле курса. 48. Шаг перетирал введённую сумму клиенту. 49. «Взять» ничего не делала. 50. Курс дня 2,85 давал ложные 9,6 %. 51. Шапка «К отправке» — одна сделка вместо пачки. 52. «6 сделки», «ждёт … назад». 53. Стенд: первая задача без уведомления; «Перейти как операционист» меняло роль; сброс открыт всем и по API; сброс не очищал доску; дубли уведомлений; «Без имени» в списке; пустой задачник советовал сменить роль.

## 6. Открытые продуктовые вопросы (не баги — фиксировать как `open`)

Старый клиент с новым объектом — допник или новый договор. Реальный допуск по сумме
прихода (сейчас 1 000 ₽ заглушкой) и что делать с недостачей ~1 000 ₽. Приходит ли
назначение из выписки Сбера целиком. TronGrid: видна ли транзакция до второй подписи,
порог подтверждений, срок жизни транзакции. Аварийный ручной перевод статуса. Уведомления
в личку или группу, топики по ролям. Поиск чата по @нику или телефону. Поля аренды
(депозит, бронь, месячная плата). Чек и AML для мелких обменов, валюта наличных. Курс
дня из api.exgreen.pro. Поле реф-метки в Битриксе. Правильные чаты Asia Capital, Coins,
Bitazza. Адрес кошелька Андрея, реальные стартовые балансы. Момент превращения задачи
в сделку. Разграничение доступа в самой CRM. Нормативы времени на шагах.

## 7. Формат отчёта QA-агента

**Как запускать.** Агентом `general-purpose` с ролью QA и прямым указанием «выполняй сам,
других агентов не запускай». `qa-tester` на пробном прогоне 24.09 передал задачу
вложенному агенту и вернулся без результата, а вложенный параллельно пошёл на тот же
порт. Один сценарий или группа — один агент, у каждого свой `RUN` (раздел 0).

Одна таблица на сценарий: `шаг · роль · действие · ожидание · факт · PASS/FAIL/open`.
В конце: список FAIL с номером сценария, скриншотом (`/tmp/qa_<S#>_<шаг>.png`) и
предложением, что чинить; отдельно — какие B-баги из списка выше подтвердились или ушли.
Вердикт прогона — через `.claude/skills/falsify/scripts/falsify.py`: сценарий = ожидание,
непройденное = `open`. Прод (`main`) не трогать, живую доску не сбрасывать.

## 8. Баги прогона 24.09

**Открытые**

| # | Где | Что видно | Ожидание |
|---|-----|-----------|----------|
| B19 | мелкие обмены пачки в минусе | баты клиенту считаются по курсу дня 2,6 без наценки, «наценка 0,3 %» только на экране; при брокере 81,40 IPPS-сделка Поляковой −$18,92 | ждёт решения Карима: выданные баты / курс клиенту в сделке (раздел 9) |
| B30 | инвойс в USD | на стенде инвойс только в батах; фрихолд через SWIFT с инвойсом застройщика в долларах (в CRM `invoice_amount_usd`, `transfer_sent_usd`) не моделируется — «USDT → USD застройщику» пройти нельзя | ждёт решения Карима: нужен ли путь «клиент USDT → SWIFT USD застройщику» до прода |
| B32 | фрихолд в CRM | задачник считает комиссию перевода 0,8 % + $50 сверху инвойса (прибыль СД-1479 $45,06), CRM — из отправленного (застройщик получает меньше, прибыль $184,80). Фрихолд на стенде — баты через Coins, в жизни — доллары SWIFT (B30) | решить вместе с B30: одна модель фрихолда в задачнике и CRM |
| B33 | агенты: задачник ≠ CRM | в CRM агенты передаются (раньше не передавались — CRM писала прибыль до выплат партнёрам), но правила расходятся: markup — задачник от прихода, CRM от max(приход, отправка); ур.2 revshare — задачник от выплаты ур.1, CRM (живая, так платит сейчас) от остатка прибыли после ур.1. Гусев, крипто-лизхолд 500 000 ฿: задачник $66,59, CRM $67,19 | решение Карима: какое правило верное; затем выровнять вторую сторону |
| B31 | крипто-приход | ~~только кнопка симуляции~~ → 25.09: настоящий хеш проверяется сервером (`/api/stand/payin/check`), приход частями, кошелёк клиента; ждёт приёмки | тестовый + основной перевод по хешам, кошелёк клиента из первого хеша, чужой отправитель не засчитывается молча |
| B20 | автовозмещение | код есть (USDT от брокера на кошелёк того же фаундера → возмещение без перевода), прогоном не проверено | пачка на кошелёк Теодора + «Вернуть фаундеру» на кошелёк Теодора → сделка и возмещение закрыты без подписи |

**Починены 24–25.09 (коммиты в `stage`) — теперь это регрессия, проверять на каждом прогоне.** Номер сохранён, чтобы ссылки из отчётов не терялись.

| # | Где | Было | Проверка |
|---|-----|------|----------|
| B1 | s26 «Оплатить инвойс» → «Вернуть с вопросом» | тост «Возвращено постановщику», в журнал пишется вопрос, но шаг остаётся s26 у операциониста, менеджеру ни задачи, ни уведомления. В `ret()` нет маршрута для s26 (есть только s5→s4, s11→s8, s24→s23) | задача уходит менеджеру на s15 «Реквизиты для оплаты», менеджеру уведомление с вопросом |
| B2 | демо СД-1479 | лежит на s18 без реквизитов (s15 пропущен) → на s26 тупик «Менеджер ещё не заполнил реквизиты» | демо-сделка на конвертации уже с заполненными реквизитами |
| B3 | s25 «Известить Coins» | сумма = доля сделки в пачке ($14 256,54), хеш выдуманный (`510e38ce…`) | сумма и хеши реально отправленного в Coins (11 217,95 USDT, ссылки с шага s23) |
| B4 | s18w «Ждём USDT», шапка | «Ждём $14 256,36 · отправлено 1 164 000 ₽» — одна сделка, и удержание посчитано как для одной | объём пачки: 1 828 458 ₽ → $22 462,62, как в блоке ниже |
| B5 | очередь «Ждут конвертации» | крипто-сделка (СД-1480, `payType: Крипта`, USDT→THB) стоит в рублёвой очереди с «—» и 0 ₽, «Взять все» добавляет её в пачку, её 29 474 ฿ входят в «должны выдать». `pendingEx` не отсекает `payCur==='usdt'` | крипто-сделки в рублёвую пачку не попадают |
| B6 | s22, «Вернуть фаундеру» по сделке с долей $0 | проходит возврат $100 без предупреждения (в поле подсказка «100») | нельзя отправить больше доли сделки, либо явное предупреждение |
| B7 | демо СД-1479 | курсы 103,50 / 31,20, курс клиенту 3,32 ₽/฿ при курсе дня 2,6 — на s18 «уходим в минус −15 %», пока не впишешь реальный курс | демо на курсах доски (81,10 / 31,20, клиенту ≈ 2,66) |
| B8 | журнал СД-1479 | «Деньги пришли … 24.09, 14:57» при засеве в 13:50 — время из будущего | время события не позже момента засева |
| B9 | s22, мелкая сделка в минусе | СД-1476: Coins просит $2 617,59 при доле $2 596,55 — экран молчит | предупреждение «сделка в минусе на $21» |
| B10 | любой чужой шаг у не-админа (подтверждён пробным прогоном S14) | «Сейчас ход роли «Операционист» — … Переключите роль наверху, чтобы продолжить» — переключателя у него нет; текст в `tasks.html` около строки 2881 | «Задача у роли X» без совета переключаться, если `standMe!=='admin'` |
| B11 | журнал s23 | «компания подписал 4 перевода» | «Виталий (фин дир) подписал…» |
| B12 | тексты | «CNV-0003 · 6 сделки», «Весь путь — 32 событий» | склонение по числу |
| B13 | STAND-README | «очередь на 754 000 ₽», на доске 670 000 ₽ | README совпадает с доской |
| B15 | чужой шаг у не-админа (Виталий, `/tasks?deal=1479` → «Текущая задача») | «Операционист» в плашке переносится посреди слова («Операционис / т»), фраза разваливается на три колонки — вёрстка `.alert` | плашка в одну строку, слова не рвутся |
| B14 | шаг «Ждём USDT», входящие | в списке входящих переводы от 21.09 и 22.09 (раньше отправки 24.09), ERC20 на TRON-кошелёк | во входящих только переводы после отправки и в сети кошелька |
| B18 | журнал s23 | «компания подписал…», затем «фин дир подписал…» с маленькой буквы | «Фин дир подписал N перевода — 1 из 2…» |
| B16 | назначения IPPS / «оставить» | баланс IPPS не уменьшался; «оставить» не записывал, откуда выдали | IPPS 1 250 000 → 1 158 462 после Поляковой; касса 480 000 → 455 385 после Мартиросяна |
| B21 | Pay-Out сделок пачки | у Coins, клиента, IPPS, «оставить» Pay-Out пустой, у возврата фаундеру — только источник; оплата инвойса затирала Pay-Out главной («конвертации не было», USDT пропали) | Pay-Out по назначению заполнен (раздел 4), у главной — Coins $11 217,95 + строка «Оплата инвойса»; прибыль считается у всех шести |
| B22 | крипто-путь, после реквизитов | нет шага «Что делаем с USDT»: Coins не назначается, у фин дира «К отправке $0,00», шаг пропускал ноль переводов, у Теодора «Одобрить $0,00» — тупик | после s15 крипта идёт на s22, назначение Coins, отправка 16 185,90 USDT, подпись, s25; s23 без переводов не пропускает |
| B23 | договор и «Отправить клиенту» у крипты | «Куда платит клиент: ООО «МФ КОРПОРЕЙШН» · ПАО Сбербанк · р/с …» — крипто-клиенту рублёвый счёт | «USDT · сеть TRC-20 · кошелёк MF Corporation …: TWBge…» |
| B24 | закрытие крипты | кнопка «Закрыть сделку» заблокирована навсегда: в чек-листе обязателен «Курс RUB→USDT», которого у крипты нет | у крипты чек-лист без RUB→USDT и хеша обмена; блокирует только crmGaps; сделка закрывается |
| B25 | шапка «Ответить курс» у крипты | «Вернуть два числа: курс брокера и курс партнёра», RUB→USDT «ждём» | «Вернуть курс USDT → THB — рублей в сделке нет» |
| B26 | валюта крипто-клиента | суммы и курсы клиента в «$» («16 181,23 $», «฿/$», «Сумма клиенту, $» в договоре) | «USDT» и «฿/USDT»; «$» остаётся только во внутренних суммах USDT учёта |
| B27 | реквизиты s15 | получатель и номер инвойса пустые, хотя в пакете уже есть (менеджер вбивал второй раз) | подставлены из пакета, если он собран |
| B28 | наценка фрихолда на расчёте | себестоимость без 0,8 % + $50 за перевод — наценка завышена | себестоимость с переводом; у лизхолда без изменений (1 % компании — доход в батах, подпись под полем) |
| B29 | крипто-приход, хеш | хеш прихода — укороченная заглушка `0x…`, список хешей прихода пуст (crmGaps: «Хеши прихода») | `demo:payin:<id>:<ts>` в payinHashes, кошелёк прихода — мультисиг Груши |
| B17 | s15 после возврата с оплаты | «Передать операционисту на оплату» вело на «Отправку брокеру» — сделка шла на второй круг конвертации | после возврата с s26 и повторной отправки реквизитов сделка снова на s26 «Оплатить инвойс», операционисту уведомление |

## 9. Вопросы к Кариму (без ответа тест пишется как `open`)

- ~~Когда закрываются мелкие обмены пачки~~ — решено 24.09, см. раздел 4 «Закрытие мелких сделок пачки».
- У мелких обменов «наценка 0,3 %», но баты клиенту считаются по курсу дня 2,6 без неё
  (238 000 ₽ → 91 538 ฿ = 238 000 / 2,6). Так задумано?
- Зачисление Coins на SCB реализовано отдельным подтверждением на s25; s26 списывает инвойс однократно. Сверка с реальной банковской выпиской остаётся ручной.
