# 📦 Что загружать в GitHub для деплоя

## Вариант 1: Вся папка `web/` (Рекомендуется)

Загрузите **только папку `web/`** в GitHub репозиторий.

### Структура репозитория:
```
your-repo/
├── .gitignore
├── api_server.py          ✅ Обязательно
├── calculator.py          ✅ Обязательно (для Doverka)
├── broker_detailed.py     ✅ Обязательно (для Broker)
├── calculator.js          ✅ Обязательно
├── index.html             ✅ Обязательно
├── styles.css             ✅ Обязательно
├── requirements.txt       ✅ Обязательно
├── Procfile               ✅ Обязательно
└── README.md              (опционально)
```

### Не загружайте:
- ❌ `__pycache__/` (добавьте в .gitignore)
- ❌ `calculator_new.js`, `calculator_old.js`
- ❌ `index_new.html`, `index_old.html`
- ❌ `.env` файлы (если есть API ключи)

---

## Вариант 2: Весь проект (если нужен бот)

Если хотите загрузить весь проект (включая бота), структура:

```
exchange_calculator_bot/
├── .gitignore
├── bot.py
├── calculator.py
├── broker_detailed.py
├── requirements.txt
├── web/
│   ├── api_server.py
│   ├── calculator.py
│   ├── broker_detailed.py
│   ├── calculator.js
│   ├── index.html
│   ├── styles.css
│   ├── requirements.txt
│   └── Procfile
└── README.md
```

Для деплоя на Railway укажите папку `web/` как корневую.

---

## Быстрая инструкция для GitHub

### 1. Создайте репозиторий на GitHub:
- Зайдите на [github.com](https://github.com)
- Создайте новый репозиторий (например, `exchange-calculator`)

### 2. Загрузите файлы:

**Через командную строку:**
```bash
# Перейдите в папку web
cd "/Users/karimamirov/Desktop/untitled folder/exchange_calculator_bot/web"

# Инициализируйте git (если ещё нет)
git init

# Добавьте файлы
git add api_server.py calculator.py broker_detailed.py calculator.js index.html styles.css requirements.txt Procfile

# Или добавьте всё (кроме игнорируемых)
git add .

# Создайте коммит
git commit -m "Initial commit - Exchange Calculator"

# Добавьте remote репозиторий (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Загрузите в GitHub
git branch -M main
git push -u origin main
```

**Через GitHub Desktop или веб-интерфейс:**
1. Создайте репозиторий на GitHub
2. Скопируйте папку `web/` в локальную папку репозитория
3. Загрузите через GitHub Desktop или веб-интерфейс

---

## После загрузки в GitHub

### Деплой на Railway:

1. Зайдите на [railway.app](https://railway.app)
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Подключите ваш GitHub аккаунт
5. Выберите репозиторий
6. Railway автоматически определит Python приложение
7. Готово! 🎉

**Важно:** Railway должен автоматически найти `Procfile` и `requirements.txt`

---

## Минимальный набор файлов

Если хотите минимум, нужны эти файлы:
- ✅ `api_server.py` - сервер
- ✅ `calculator.py` - логика Doverka
- ✅ `broker_detailed.py` - логика Broker  
- ✅ `calculator.js` - фронтенд
- ✅ `index.html` - HTML
- ✅ `styles.css` - стили
- ✅ `requirements.txt` - зависимости Python
- ✅ `Procfile` - команда запуска для Railway

**Всё остальное опционально!**

